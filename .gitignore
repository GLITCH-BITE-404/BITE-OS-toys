# Nothing here is generated by hand — `bite-toys pack` rebuilds it all.
# The tarballs and catalog.tsv ARE the published artifacts, so they are
# deliberately committed, not ignored.
*.tmp
__pycache__/
